spring.application.name=TrabajoPracticoDesarrollo

# --- CONEXI�N A BASE DE DATOS NEON ---
# Ejemplo (Aseg�rate de mantener tu URL original de Neon, solo agrega el final)
spring.datasource.url=jdbc:postgresql://ep-red-truth-aczpiy6y-pooler.sa-east-1.aws.neon.tech:5432/neondb?sslmode=require&stringtype=unspecified
spring.datasource.username=neondb_owner
spring.datasource.password=npg_QufXyUrW7aL9
spring.datasource.driver-class-name=org.postgresql.Driver

# --- CONFIGURACI�N JPA / HIBERNATE ---
# Muestra las consultas SQL en la consola (�til para debug)
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# 'update': Actualiza la estructura de tablas si cambias algo en Java.
# 'validate': Solo verifica que coincidan.
# 'none': No hace nada (usa solo tu script SQL).
spring.jpa.hibernate.ddl-auto=none

# Dialecto para PostgreSQL (mejora la compatibilidad)
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Configuración para respetar nombres de columnas con mayúsculas
spring.jpa.hibernate.naming.physical-strategy=org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl

# --- CONFIGURACIÓN DEL SERVIDOR ---
# Puerto en el que corre el backend
server.port=8080