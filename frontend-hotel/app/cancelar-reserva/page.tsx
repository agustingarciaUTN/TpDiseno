import { CancelarReservaForm } from "@/components/cancelar-reserva-form"

export default function CancelarReservaPage() {
    return (
        // Renderizamos directamente el componente, que ya se encarga de todo el diseño
        <CancelarReservaForm />
    )
}